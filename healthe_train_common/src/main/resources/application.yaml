server:
  port: 8800
spring:
  application:
    name: common

healthe:
  auth:
    exclude-paths:

    include-paths:
  jwt:
    location: classpath:healthe.jks
    alias: healthe
    password: healthe
    tokenTTL: 10